import { FileIconConfig } from './FileIcon.styles';
import { BaseProps } from '../../types';
type StylesBuilderParams = {
    container: BaseProps;
};
export declare const stylesBuilder: (custom?: FileIconConfig) => StylesBuilderParams;
export {};
