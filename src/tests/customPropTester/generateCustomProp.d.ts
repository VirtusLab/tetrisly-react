import { Options } from './options.type';
export declare const generateCustomProp: ({ innerElements, props, }: Options) => {
    innerElements: {
        [x: string]: object;
    };
    borderTopColor: string;
};
